<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user ? 'Editar ' : 'Crear ' %>usuario</title>
</head>
<body>
    <h1>Formulario de <%= user ? 'Editar ' : 'Crear ' %>usuario</h1>

    <form action="<%= user ? '/update/' + user._id : '/store' %>" method="post">
        
        <label for="name">Nombre</label>
        <input type="text" id="name" name="name" value="<%= user ? user.name : '' %>" required>
        <br>            
        <label for="email">Email</label>
        <input type="email" id="email" name="email" value="<%= user ? user.email : '' %>" required>
        <br>
        <label for="age">Edad</label>
        <input type="number" id="age" name="age" value="<%= user ? user.age : '' %>" required>
        <br>
        <button type="submit"><%= user ? 'Editar ' : 'Crear ' %></button>
    </form>
</body>
</html>
